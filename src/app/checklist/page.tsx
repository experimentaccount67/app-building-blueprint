import ChecklistApp from '@/components/ChecklistApp'

export const metadata = {
  title: 'App Building Blueprint — Checklist',
  description: 'Your production-ready checklist. Check off every layer before you ship.',
}

export default function ChecklistPage() {
  return <ChecklistApp />
}
